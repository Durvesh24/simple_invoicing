# Backend Environment Variables Template
# Copy this file to .env.development or .env.production and update values

# Database Configuration
# For local development: postgresql://respawn_user:respawn_password@localhost:5432/respawn_invoicing
# For Docker: postgresql://respawn_user:respawn_password@db:5432/respawn_invoicing
DATABASE_URL=postgresql://respawn_user:respawn_password@localhost:5432/respawn_invoicing

# JWT Configuration
# Use a strong, random string for production (minimum 32 characters)
SECRET_KEY=dev-secret-key-change-in-production

# JWT Algorithm
ALGORITHM=HS256

# Token expiration in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Debug mode (true for development, false for production)
DEBUG=true

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Optional: CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:5173,https://invoicing.nikhilbhatia.com

# Optional: Application environment
# ENVIRONMENT=development
